

In the landing page:

1. make sure everything is proper aligned and all the sections are properly accounted for

2. the projectslider should still be present but match the new design 

3. revamp every card in the site to a similar foprmat matching the new design to keep consistency

4. keep the home projects section as is but remeber to also revamp the buttons to match

5. ensure you also revamp the projects page and the projectid page too

6. do not change the nav links present but change thenav design to match the new design

7.keep the mystack section but change the design of it to match the new design (i like the button ux i did)

8.  use gsap for the work experience section, so when a user reaches that section and tries to scroll down, instead pf scrolling down, the work experiences will be in cards each and when a user scrolls it will scroll from right to left. so the cards will be on the furthest right (with no overflow) layered on top of eachother like a scattered card deck, as the userscrolls down, each card will leave the card deck and start to seperate one by one with a set gap. then when the last card reaches the center ofthe screen, we will resume scroll as usual
"https://demos.gsap.com/demo/horizontal-scrolling-gallery/" this link contains a preview of how it should look
also make sure the information used in this section is pulled from a data file so i can edit and add multiple

9. keep my testimonal cards and the card deck animation, just style the entire section to match the redesign

10. revamp the entire contacts setion ansd the hire me modal